quarkus.application.name=communication-service
quarkus.http.port=8080

quarkus.datasource.db-kind=postgresql
quarkus.datasource.jdbc.url=${DB_URL:jdbc:postgresql://localhost:5432/crm_communication}
quarkus.datasource.username=${DB_USERNAME:crm}
quarkus.datasource.password=${DB_PASSWORD:crm}
quarkus.hibernate-orm.database.generation=none

# ── Flyway ────────────────────────────────────────────────────────────────────
quarkus.flyway.migrate-at-start=true
quarkus.flyway.baseline-on-migrate=true
quarkus.flyway.baseline-version=1.0.0
quarkus.flyway.table=flyway_communication_history
quarkus.kafka.bootstrap.servers=${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
mp.messaging.connector.smallrye-kafka.bootstrap.servers=${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
quarkus.kafka.health.enabled=false
quarkus.smallrye-health.root-path=/health

mp.messaging.outgoing.communication-events.connector=smallrye-kafka
mp.messaging.outgoing.communication-events.topic=crm.communication.events
mp.messaging.outgoing.communication-events.value.serializer=io.quarkus.kafka.client.serialization.ObjectMapperSerializer

mp.messaging.outgoing.domain-events.connector=smallrye-kafka
mp.messaging.outgoing.domain-events.topic=crm.domain-events
mp.messaging.outgoing.domain-events.value.serializer=io.quarkus.kafka.client.serialization.ObjectMapperSerializer

quarkus.opentelemetry.enabled=true
quarkus.opentelemetry.tracer.exporter.otlp.endpoint=${OTEL_EXPORTER_OTLP_ENDPOINT:http://localhost:4317}
quarkus.opentelemetry.tracer.sampler=always_on
quarkus.opentelemetry.tracer.ratio=1.0
quarkus.log.console.format=%d{HH:mm:ss} %-5p traceId=%X{traceId}, spanId=%X{spanId}, traceFlags=%X{traceFlags} [%c{2.}] (%t) %s%e%n
quarkus.micrometer.export.prometheus.path=/metrics

%k8s.quarkus.oidc.enabled=false
%k8s.quarkus.http.auth.proactive=false
%k8s.quarkus.opentelemetry.enabled=false
