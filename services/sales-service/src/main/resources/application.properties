quarkus.application.name=sales-service
quarkus.http.port=8082

quarkus.datasource.db-kind=postgresql
quarkus.datasource.jdbc.url=${DB_URL:jdbc:postgresql://localhost:5432/crm_sales}
quarkus.datasource.username=${DB_USERNAME:crm}
quarkus.datasource.password=${DB_PASSWORD:crm}
quarkus.hibernate-orm.database.generation=validate

# ── Flyway ────────────────────────────────────────────────────────────────────
quarkus.flyway.migrate-at-start=true
quarkus.flyway.baseline-on-migrate=true
quarkus.flyway.baseline-version=1.0.0

mp.messaging.outgoing.sales-events.connector=smallrye-kafka
mp.messaging.outgoing.sales-events.topic=crm.sales.events
mp.messaging.outgoing.sales-events.value.serializer=io.quarkus.kafka.client.serialization.ObjectMapperSerializer

mp.messaging.outgoing.domain-events.connector=smallrye-kafka
mp.messaging.outgoing.domain-events.topic=crm.domain-events
mp.messaging.outgoing.domain-events.value.serializer=io.quarkus.kafka.client.serialization.ObjectMapperSerializer

mp.messaging.incoming.customer-events.connector=smallrye-kafka
mp.messaging.incoming.customer-events.topic=crm.ciam.events
mp.messaging.incoming.customer-events.value.deserializer=org.apache.kafka.common.serialization.StringDeserializer

# ── Dead Letter Queue ──────────────────────────────────────────────────────
# On persistent failure, route to DLQ instead of blocking the partition.
mp.messaging.incoming.customer-events.failure-strategy=dead-letter-queue
mp.messaging.incoming.customer-events.dead-letter-queue.topic=crm.ciam.events.dlq
mp.messaging.incoming.customer-events.dead-letter-queue.key.serializer=org.apache.kafka.common.serialization.StringSerializer
mp.messaging.incoming.customer-events.dead-letter-queue.value.serializer=org.apache.kafka.common.serialization.StringSerializer
mp.messaging.incoming.customer-events.dead-letter-queue.max-retries=3
mp.messaging.incoming.customer-events.dead-letter-queue.delay=2s

# ── Observability ──────────────────────────────────────────────────────────
# OpenTelemetry distributed tracing — unified configuration
quarkus.opentelemetry.enabled=true
quarkus.opentelemetry.tracer.exporter.otlp.endpoint=${OTEL_EXPORTER_OTLP_ENDPOINT:http://localhost:4317}
quarkus.opentelemetry.tracer.sampler=always_on
quarkus.opentelemetry.tracer.ratio=1.0

# Console log format with trace correlation
quarkus.log.console.format=%d{HH:mm:ss} %-5p traceId=%X{traceId}, spanId=%X{spanId}, traceFlags=%X{traceFlags} [%c{2.}] (%t) %s%e%n

# ── Kubernetes profile ─────────────────────────────────────────────────────
%k8s.quarkus.oidc.enabled=false
%k8s.quarkus.http.auth.proactive=false
