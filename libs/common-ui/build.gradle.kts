/*
 * :libs:common-ui
 *
 * Shared UI templates and resources for all CRM services.
 * Provides the base Qute layout template with sidebar navigation,
 * dark theme CSS, htmx integration, and common badge/table/pagination styles.
 */

plugins {
    id("crm.kotlin-convention")
}
