apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-communication
  namespace: crm
spec:
  serviceName: postgres-communication
  replicas: 1
  selector:
    matchLabels:
      app: postgres-communication
  template:
    metadata:
      labels:
        app: postgres-communication
    spec:
      initContainers:
        - name: init-schema
          image: postgres:17-alpine
          command:
            - sh
            - -c
            - |
              until pg_isready -h localhost -U crm -d crm; do sleep 1; done
              psql -U crm -d crm -c 'CREATE SCHEMA IF NOT EXISTS communication; GRANT ALL PRIVILEGES ON SCHEMA communication TO crm;'
          env:
            - name: PGPASSWORD
              value: crm
      containers:
        - name: postgres
          image: postgres:17-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: crm
            - name: POSTGRES_USER
              value: crm
            - name: POSTGRES_PASSWORD
              value: crm
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "250m"
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: standard
        resources:
          requests:
            storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-communication
  namespace: crm
spec:
  selector:
    app: postgres-communication
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None
