apiVersion: apps/v1
kind: Deployment
metadata:
  name: marketing-service
  namespace: crm
spec:
  replicas: 1
  selector:
    matchLabels:
      app: marketing-service
  template:
    metadata:
      labels:
        app: marketing-service
    spec:
      containers:
        - name: marketing-service
          image: ghcr.io/laughingjackalope/marketing-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8085
          env:
            - name: QUARKUS_DATASOURCE_JDBC_URL
              value: "jdbc:postgresql://postgres-marketing:5432/crm"
            - name: QUARKUS_DATASOURCE_USERNAME
              value: "crm"
            - name: QUARKUS_DATASOURCE_PASSWORD
              value: "crm"
            - name: QUARKUS_KAFKA_BOOTSTRAP_SERVERS
              value: "kafka:9092"
            - name: QUARKUS_OIDC_ENABLED
              value: "false"
            - name: QUARKUS_HTTP_AUTH_PROACTIVE
              value: "false"
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8085
            initialDelaySeconds: 30
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: 8085
            initialDelaySeconds: 15
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: marketing-service
  namespace: crm
spec:
  selector:
    app: marketing-service
  ports:
    - port: 8085
      targetPort: 8085
  type: ClusterIP
